sudo: false
language: node_js
node_js:
  - '14'
branches:
  only:
    - master
cache:
  directories:
    - node_modules
before_install:
  - yarn update
install:
  - yarn install
script:
  - yarn test
